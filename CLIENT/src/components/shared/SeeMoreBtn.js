import React from "react";
import Button from "./Button";

const SeeMoreBtn = () => {
    return (
        <div className=" flex justify-center mb-6">
            <Button btnPath="/" btnName="See more blogs" />
        </div>
    );
};

export default SeeMoreBtn;
